# 解析器回归测试 — 未覆盖功能点

> 生成日期：2026-06-20 | 当前覆盖率：36/61 功能点（38 个用例） | 未覆盖：25 个

---

## 概述

回归测试目录 `test/native-parser-regression/` 当前有 38 个用例，覆盖了 36 个解析器功能点。解析器源码实际支持约 61 个独立功能点，缺口约 25 个。

**新增用例方式：**
- 在 `samples/` 放 `.txt`（纯文本输入）
- 在 `expected/` 放 `.json`（预期输出，可用现有 expected 文件作模板）
- 在 `manifest.json` 加对应条目（`id`、`mode`、`sample`、`expected`）
- 新增 `expected/` 后，`actual/` 会自动生成参考输出，确认无误后复制到 `expected/` 即可

---

## 一、答案解析规则（AnswerParser.kt）— 已补充 4 个双文件答案侧回归

> 已补充用例：`30_answer_table_format`、`31_range_answer_format`、`32_compact_answer_line`、`33_inline_multi_answer`。
> 这些能力当前稳定覆盖在“双文件导入 / 答案文件侧”场景中；不要误写成“单文件题库正文开头直接放答案表”的主回归预期。

### 1.1 答案表格式（已覆盖）

**代码：** `AnswerParser.kt:parseTableAnswerRule`

**功能：** 识别"题号: 1 2 3 4 / 答案: A B C D"双行表格式

**样例输入：**
```text
题号: 1 2 3 4 5
答案: A B C D A

1. 题目一
A. 选项A
B. 选项B
...
5. 题目五
A. 选项A
B. 选项B
```

**已覆盖用例 ID：** `30_answer_table_format`

---

### 1.2 范围答案格式（已覆盖）

**代码：** `AnswerParser.kt:parseRangeAnswerRule`

**功能：** 识别 "1-5: A B C D E" 范围缩写答案

**样例输入：**
```text
1-5: A B A C D

一、单选题
1. 题目一
A. 选项A
B. 选项B
```

**已覆盖用例 ID：** `31_range_answer_format`

---

### 1.3 紧凑答案行（已覆盖）

**代码：** `AnswerParser.kt:parseCompactAnswerLineRule`

**功能：** 识别 "1A2B3C4D5E" 无分隔符答案（需 5 对以上触发）

**样例输入：**
```text
1A2B3C4D5E

一、单选题
1. 题目一
A. 选项
B. 选项
...共 5 题
```

**已覆盖用例 ID：** `32_compact_answer_line`

---

### 1.4 行内多条目答案（已覆盖）

**代码：** `AnswerParser.kt:parseInlineMultiAnswerRule`

**功能：** 识别同一行多组 "1.A 2.B 3.C" 格式

**样例输入：**
```text
1.A 2.B 3.C 4.D 5.A

1. 题目一
A. 选项A
B. 选项B
```

**已覆盖用例 ID：** `33_inline_multi_answer`

---

## 二、选项与格式推断（StandardQuestionParser.kt）— 已补充 2 个文本回归，仍有 2 个未覆盖/暂不强制

> 已补充用例：`34_solution_choice_extraction`、`36_plain_option_inference`。
> `35_embedded_stem_answer` 当前暂不纳入支持范围；图片选项需要图片上下文，纯文本回归暂不强制覆盖。

### 2.1 "答案：应选A" 行内答案提取（已覆盖）

**代码：** `StandardQuestionParser.kt:solutionChoiceRegex`

**功能：** 题干/解析末尾 "答案：应选A" / "故选B" / "分析：选C" 自动提取正确答案

**样例输入：**
```text
1. 关于依法行政，下列说法正确的是：
A. 选项A
B. 选项B
C. 选项C
D. 选项D
分析：选B。其他选项不符合依法行政的要求。
```

**已覆盖用例 ID：** `34_solution_choice_extraction`

---

### 2.2 题干括号内嵌答案提取

**代码：** `StandardQuestionParser.kt:extractEmbeddedAnswerFromStem`

**功能：** 选择题干中"（C）"或"(D)"等括号内单选字母自动识别为答案

**样例输入：**
```text
1. （C）根据《行政诉讼法》，下列说法正确的是：
A. 选项A
B. 选项B
C. 选项C
D. 选项D
```

**状态：** 当前暂不纳入支持范围，暂不放入回归 manifest。

---

### 2.3 图片选项识别

**代码：** `StandardQuestionParser.kt:inferImageOptionLabelLine`

**功能：** 题目标记为图片题时，从"A B C D"标记行推断选项键

> 此场景需要图片上下文，回归测试中难以纯文本模拟。暂不强制覆盖。

---

### 2.4 无标记纯文本选项推断（已覆盖）

**代码：** `StandardQuestionParser.kt:inferPlainOptionLines`

**功能：** 没有 A. 前缀的选项行自动推断为选项键

**样例输入：**
```text
1. 选择正确的一项
选项一
选项二
选项三
答案：A
```

**已覆盖用例 ID：** `36_plain_option_inference`

---

## 三、区块分割边缘格式（QuestionBlockSplitter.kt）— 已补充 3 个文本回归，仍有 3 个未覆盖/待支持

> 已补充用例：`37_interview_question_number`、`38_glued_question_number`、`40_material_intro_skip`。
> `39_subjective_answer_continuation` 需要重新设计样例和解析预期，暂不放入当前已通过回归。

### 3.1 面试题 / 中文序号开头（已覆盖）

**代码：** `QuestionBlockSplitter.kt:interviewQuestionStartRegex`

**功能：** 识别"问题一""题目一""第一题"等中文题号

**样例输入：**
```text
问题一：请简述依法行政的基本要求。
参考要点：合法行政、合理行政、程序正当、高效便民、诚实守信、权责统一。

问题二：简述行政公开原则的意义。
参考要点：保障公民知情权、加强对行政权力的监督、促进政府依法行政、预防腐败。
```

**已覆盖用例 ID：** `37_interview_question_number`

---

### 3.2 粘合题号格式（已覆盖）

**代码：** `QuestionBlockSplitter.kt:gluedQuestionStartRegex`

**功能：** 识别数字与题干直接粘连（"1题干" 无空格）的题号

**样例输入：**
```text
1下列说法正确的是：
A. 选项A
B. 选项B

2下列哪项是错误的：
A. 选项A
B. 选项B
```

**已覆盖用例 ID：** `38_glued_question_number`

---

### 3.3 无编号题目自动编号

**代码：** `QuestionBlockSplitter.kt:isLikelyUnnumberedQuestionLine`

**功能：** 纯题干行无题号前缀时自动分配序号

> 此场景高度依赖上下文，需搭配分区标题使用。可嵌套在分区继承用例中。

---

### 3.4 主观答案延续行

**代码：** `QuestionBlockSplitter.kt:shouldKeepAsSubjectiveAnswerContinuation`

**功能：** 简答题后紧跟子编号（1. 2. 3.）内容时，归属到上一题的答案区而非新题

**样例输入：**
```text
1. 请简述提升政务服务水平的措施。
参考答案：
1. 完善服务平台，推进线上线下融合
2. 优化办事流程，精简审批环节
3. 加强人员培训，提升服务质量

2. 简述政务公开的基本原则。
参考答案：坚持公开为常态、不公开为例外。
```

**状态：** 当前样例会暴露解析器边界问题，等待重新整理样例后再纳入回归。

---

### 3.5 材料导语行跳过（已覆盖）

**代码：** `QuestionBlockSplitter.kt:isMaterialIntroLine`

**功能：** "材料一："等导语行不被误判为题目

**样例输入：**
```text
材料一：近年来，某市大力推进"互联网+政务服务"改革，取得显著成效。
材料二：《行政许可法》实施以来，各地不断简化审批流程。

1. 材料一反映了政务服务的哪种趋势？
A. 线上替代线下
B. 线上线下融合
C. 线下为主
D. 完全自动化
```

**已覆盖用例 ID：** `40_material_intro_skip`

---

### 3.6 独立数字表格值防护

**代码：** `QuestionBlockSplitter.kt:looksLikeStandaloneNumericTableValue`

**功能：** 纯数字表格值（如 "12345"）不被误判为题号

**样例输入：**
```text
2023年某市政务服务中心办件量统计：
总办件量：12345
同比增长：15.6%

1. 根据以上数据，办件量增长的主要原因是：
A. 人口增长
B. 流程优化
C. 政策变化
D. 经济因素
```

**建议用例 ID：** `41_numeric_table_value_guard`

---

## 四、文本正常化（QuestionTextNormalizer.kt）— 2 个未覆盖

### 4.1 全角 ASCII 正常化

**代码：** `QuestionTextNormalizer.kt:normalizeFullWidthAscii`

**功能：** 全角字母/数字转半角（Ａ→A、１→1）

**样例输入：**
```text
1. 以下关于行政法的说法，正确的是：
A. 行政法是调整行政关系的法律规范的总称
B. 行政法只调整行政机关与公民的关系
C. 行政法不具有强制性
D. 行政法只适用于行政处罚领域
答案：Ａ　　　解析：Ａ选项正确。
```

**建议用例 ID：** `42_fullwidth_ascii_normalize`

---

### 4.2 个人资料表头剥离

**代码：** `QuestionTextNormalizer.kt:stripProfileHeaderBeforeQuestion`

**功能：** 导入文本开头"姓名:""单位:""准考证号:"等行自动去除

**样例输入：**
```text
姓名：张三
单位：某某学院
准考证号：20240001

一、单选题
1. 行政法的基本原则不包括：
A. 合法行政
B. 合理行政
C. 效率优先
D. 程序正当
```

**建议用例 ID：** `43_profile_header_strip`

---

## 五、文件格式解码层（TextImportDecoder.kt）— 5 个未覆盖

> 这些场景依赖二进制文件（.docx/.xlsx），回归测试框架目前只支持纯文本输入。
> 建议方案：用真实文件做手工验证，或扩展 runner 支持二进制文件输入。

### 5.1 DOCX 文件解码

**代码：** `TextImportDecoder.kt:decodeDocxDetailed`

**功能：** 解压 ZIP、读取 word/document.xml，提取段落文本与图片

--- 

### 5.2 XLSX 文件解码

**代码：** `TextImportDecoder.kt:decodeXlsxDetailed`

**功能：** 读取 Shared Strings、工作表单元格

---

### 5.3 OMML 数学公式提取

**代码：** `TextImportDecoder.kt:extractOmmlText`（239-320 行）

**功能：** 分数、根号、三角函数、矩阵、累加、上下标、定界符、重音、长条

---

### 5.4 GB18030 编码回退

**代码：** `TextImportDecoder.kt:decodePlainText`（58-66 行）

**功能：** UTF-8 解码含 `�` 时自动回退 GB18030

---

### 5.5 Spreadsheet 富文本与上下标

**代码：** `TextImportDecoder.kt:extractSpreadsheetRichText` + `detectVertAlign`

---

## 六、表格解析扩展（ExcelQuestionTableParser.kt）— 5 个未覆盖

> 当前仅有 09_table_text.tsv 一个表格用例（TSV 格式）

### 6.1 CSV 引号字段

**代码：** `ExcelQuestionTableParser.kt:parseCsvLine`

**样例输入（CSV）：**
```csv
number,question,option_a,option_b,option_c,option_d,answer
1,"下列选项中,属于行政许可的是",营业执照申请,税收征收,行政处罚,行政拘留,A
```

---

### 6.2 合并选项列

**代码：** `ExcelQuestionTableParser.kt:parseCombinedOptions`

**功能：** 单列内 "A.xxx B.yyy C.zzz" 自动拆分

---

### 6.3 多表头区域检测

**代码：** `ExcelQuestionTableParser.kt:findHeaderMapping`

---

### 6.4 题型列映射

**代码：** `ExcelQuestionTableParser.kt:parseQuestionType`

---

### 6.5 分值/分数列

**代码：** `ExcelQuestionTableParser.kt` scoreColumn

---

## 七、章节与分区（ChapterScopeParser / FullPaperFallbackStrategy）— 4 个未覆盖

### 7.1 章节标题解析

**代码：** `ChapterScopeParser.kt`

**样例输入：**
```text
第一章 行政法概述
1. 行政法的概念是什么？
A. 选项A
B. 选项B
```

**建议用例 ID：** `44_chapter_scope_parse`

---

### 7.2 整卷分区别名归一化

**代码：** `FullPaperSectionParser.kt:canonicalName`

**功能：** "常识"→"常识判断"，"言语"→"言语理解" 等映射

---

### 7.3 隐式分区编号

**代码：** `FullPaperFallbackStrategy.kt:assignImplicitSectionCategoriesForFullPaper`

---

### 7.4 密排行修复

**代码：** `FullPaperFallbackStrategy.kt:repairDenseQuestionLine`

---

## 八、双文件合并（DualFileMerger.kt）— 2 个未覆盖

### 8.1 章节范围合并

**代码：** `DualFileMerger.kt:mergeByChapterAndNumber`

---

### 8.2 题型分组合并

**代码：** `DualFileMerger.kt:mergeByTypeAndNumber`

---

## 汇总

| 优先级 | 组 | 未覆盖数 | 可文本测试 | 建议新增用例 |
|--------|-----|---------|-----------|------------|
| ⭐⭐⭐ | 一、答案解析规则 | 4 | 4 | 30-33 |
| ⭐⭐⭐ | 二、选项与格式推断 | 4 | 2 | 34-36 |
| ⭐⭐⭐ | 三、区块分割边缘格式 | 6 | 4 | 37-41 |
| ⭐⭐ | 四、文本正常化 | 2 | 2 | 42-43 |
| ⭐ | 七、章节与分区 | 4 | 1 | 44 |
| — | 五、文件格式解码 | 5 | 0（需二进制） | — |
| — | 六、表格解析扩展 | 5 | 0（需 CSV/XLSX） | — |
| — | 八、双文件合并 | 2 | 1（需构造多卷） | — |

**优先做：** 一、二、三、四组（12 个纯文本用例，成本最低）。
**逐步做：** 七组、八组（需更复杂的输入构造）。
**暂不做：** 五、六组（需扩展 runner 或手工验证）。
